<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items.order_by_created_at_desc %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>
